import Firebase from './firebase'
import FirebaseContext, { withFirebase, useFirebase } from './context'
import * as PATHS from './PATHS'

export default Firebase

export { FirebaseContext, withFirebase, PATHS, useFirebase }
